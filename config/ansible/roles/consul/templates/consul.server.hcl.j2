advertise_addr="{{listen_ip}}"
bind_addr="0.0.0.0"
node_name="{{type}}-{{listen_ip | regex_replace("\.", "-")}}"

server=true
bootstrap_expect={{num_servers}}
retry_join = {{server_ips | to_json }}
retry_interval = "30s"


addresses {
    dns = "10.0.0.2 {{listen_ip}} 127.0.0.1"
}